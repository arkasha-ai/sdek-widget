# 2026-03-21 (суббота)

## Сессия ~00:15 - Dokploy SearXNG mount

**Задача:** Примонтировать settings.yaml для SearXNG чтобы настройки сохранялись.

**SearXNG:** `applicationId: DYAiDlAJ-uOQJ6o3OIHOt`, appName: `jakeberrimor-searxng-mpquol`  
**Проект:** `jakeBerrimor` (`SpE5kyI9gil813PJFvRA2`)

**Исследование API:**
- Endpoint `mounts.create` работает (POST с API key)
- Попытка `type: file` → ошибка `EISDIR` (директория `/etc/dokploy/applications/jakeberrimor-searxng-mpquol/files` уже существует как папка, не файл)
- Попытка `type: bind` (hostPath: `/opt/searxng/settings.yml`) → создан, но файл на хосте не существует
- Попытка `type: volume` (volumeName: `searxng-settings`) → создан

**Статус на момент прерывания:** Сессия прервана пользователем. Было создано несколько тестовых mountов (возможно с дублями). Нужно проверить через UI и очистить, затем правильно настроить.

**Email:** Build failed for dokploy — вероятно неудачный деплой SearXNG из-за экспериментов с mounts.

