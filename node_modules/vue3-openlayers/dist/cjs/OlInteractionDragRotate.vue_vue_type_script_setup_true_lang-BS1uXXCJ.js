"use strict";
const vue = require("vue");
const DragRotate = require("ol/interaction/DragRotate");
const composables_usePropsAsObjectProperties = require("./composables/usePropsAsObjectProperties.js");
const _sfc_main = /* @__PURE__ */ vue.defineComponent({
  __name: "OlInteractionDragRotate",
  props: {
    condition: { type: Function },
    duration: {}
  },
  setup(__props, { expose: __expose }) {
    const props = __props;
    const map = vue.inject("map");
    const properties = composables_usePropsAsObjectProperties.default(props);
    const dragrotate = vue.computed(() => {
      const s = new DragRotate(properties);
      return s;
    });
    vue.watch(dragrotate, (newVal, oldVal) => {
      map?.removeInteraction(oldVal);
      map?.addInteraction(newVal);
      map?.changed();
    });
    vue.onMounted(() => {
      map?.addInteraction(dragrotate.value);
    });
    vue.onUnmounted(() => {
      map?.removeInteraction(dragrotate.value);
    });
    __expose({
      dragrotate
    });
    return (_ctx, _cache) => {
      return vue.renderSlot(_ctx.$slots, "default");
    };
  }
});
exports._sfc_main = _sfc_main;
