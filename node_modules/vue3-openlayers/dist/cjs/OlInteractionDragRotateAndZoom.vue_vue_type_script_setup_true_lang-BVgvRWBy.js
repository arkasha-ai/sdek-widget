"use strict";
const vue = require("vue");
const DragRotateAndZoom = require("ol/interaction/DragRotateAndZoom");
const composables_usePropsAsObjectProperties = require("./composables/usePropsAsObjectProperties.js");
const _sfc_main = /* @__PURE__ */ vue.defineComponent({
  __name: "OlInteractionDragRotateAndZoom",
  props: {
    condition: { type: Function },
    duration: {}
  },
  setup(__props, { expose: __expose }) {
    const props = __props;
    const map = vue.inject("map");
    const properties = composables_usePropsAsObjectProperties.default(props);
    const dragRotateZoom = vue.computed(() => new DragRotateAndZoom(properties));
    vue.watch(dragRotateZoom, (newVal, oldVal) => {
      map?.removeInteraction(oldVal);
      map?.addInteraction(newVal);
      map?.changed();
    });
    vue.onMounted(() => {
      map?.addInteraction(dragRotateZoom.value);
    });
    vue.onUnmounted(() => {
      map?.removeInteraction(dragRotateZoom.value);
    });
    __expose({
      dragRotateZoom
    });
    return (_ctx, _cache) => {
      return vue.renderSlot(_ctx.$slots, "default");
    };
  }
});
exports._sfc_main = _sfc_main;
