"use strict";
Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: "Module" } });
const vue = require("vue");
const composables_usePropsAsObjectProperties = require("./usePropsAsObjectProperties.js");
function useAnimation(AnimationType, props) {
  const map = vue.inject("map");
  const vectorLayer = vue.inject("vectorLayer");
  const properties = composables_usePropsAsObjectProperties.default(props);
  const animation = vue.computed(() => new AnimationType(properties));
  vue.provide("animation", animation);
  return {
    map,
    vectorLayer,
    animation,
    properties
  };
}
exports.default = useAnimation;
